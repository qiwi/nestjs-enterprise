{
  "name": "@qiwi/nestjs-enterprise-mdc",
  "version": "1.4.1",
  "description": "MDC module for nestjs enterprise",
  "publishConfig": {
    "access": "public"
  },
  "main": "./target/es6/index.js",
  "exports": "./target/es6/index.js",
  "source": "target/ts/index.ts",
  "types": "target/es6/index.d.ts",
  "typescript": {
    "definition": "target/es6/index.d.ts"
  },
  "type": "module",
  "files": [
    "README.md",
    "CHANGELOG.md",
    "target",
    "typings",
    "flow-typed",
    "buildstamp.json"
  ],
  "scripts": {
    "test": "yarn lint && yarn test:spec",
    "test:gen": "yarn jest --updateSnapshot",
    "test:spec": "yarn jest",
    "jest": "yarn infra:jest -w=1 --config=jest.config.json",
    "format": "yarn infra:prettier --write \"src/**/*.ts\"",
    "lint": "yarn infra:eslint src/**/*.ts",
    "lint:fix": "yarn lint --fix",
    "clean": "yarn infra:rimraf target typings buildcache docs coverage flow-typed buildstamp.json",
    "build": "yarn prebuild && yarn build:es6 && yarn build:ts && yarn docs && yarn uglify",
    "prebuild": "cp ../../buildstamp.json ./",
    "build:es6": "mkdir -p target/es6 && yarn infra:tsc -p tsconfig.es6.json && yarn infra:tsc-esm-fix --target='target/es6'",
    "build:ts": "cp -r src/main/ts/ target/ts/",
    "docs": "yarn infra:typedoc",
    "uglify": "yarn infra:uglify",
    "postupdate": "yarn && yarn build && yarn test"
  },
  "dependencies": {
    "@nestjs/common": "^9.0.5",
    "@nestjs/core": "^9.0.5",
    "@qiwi/mware": "^1.13.1",
    "@qiwi/mware-context": "^1.13.0",
    "@qiwi/substrate": "^1.20.14",
    "rxjs": "^7.5.6",
    "tslib": "^2.4.0"
  },
  "devDependencies": {
    "@qiwi/nestjs-enterprise-infra": "workspace:*",
    "esm": "^3.2.25",
    "reflect-metadata": "^0.1.13"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/qiwi/nestjs-enterprise.git"
  },
  "keywords": [
    "js platform",
    "config"
  ],
  "bugs": {
    "url": "https://github.com/qiwi/nestjs-enterprise/issues"
  },
  "homepage": "https://github.com/qiwi/nestjs-enterprise#readme"
}
